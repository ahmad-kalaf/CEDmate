import 'package:flutter/material.dart';

class StuhlgangNotieren extends StatelessWidget {
  const StuhlgangNotieren({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Stuhlgang Notieren'),
      ),
      body: Center()
    );
  }
}
