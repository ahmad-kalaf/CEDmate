import 'package:flutter/material.dart';

class MahlzeitEintragen extends StatelessWidget {
  const MahlzeitEintragen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Mahlzeit eintragen'),
      ),
      body: const Center()
    );
  }
}
