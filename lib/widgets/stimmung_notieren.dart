import 'package:flutter/material.dart';

class StimmungNotieren extends StatelessWidget {
  const StimmungNotieren({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Stimmung'),
      ),
    );
  }
}
