import 'package:flutter/material.dart';

class SymptomErfassen extends StatelessWidget {
  const SymptomErfassen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Symptom erfassen'),
      ),
      body: const Center()

    );
  }
}
